<!DOCTYPE html>
<html>
  <%- include('head.ejs', {title: 'Pedir passe'}); %>

  <body>
    <div class="container">
        <header>
          <%- include('navbar.ejs'); %>
        </header>

        <div class="mt-3 p-3 rounded border bg-light">
          <% if(hasPass == false) { %>
            <% if(getPass == false) { %>
              <a href="/getPass" class="btn btn-outline-dark mt-3" role="button">Pedir passe</a>
            <% } else { %>
              <% if(emittedPass == false) { %>
                <h3 class="text-center mt-3">A emisão do passe ainda está em análise</h3>
              <% } %>
            <% } %>
          <% } else { %>
            <a href="#" class="btn btn-outline-dark btn-lg btn-block mt-3" role="button">Pagar próximo mês</a>
            <label class="mt-3">Tipo de passe: <%= typePass %></label></br>
            <a href="/getPass/tipo" class="btn btn-outline-dark mt-1" role="button">Mudar tipo de passe</a>
            <a href="#" class="btn btn-outline-dark btn-lg btn-block mt-3" role="button">Obter comprovativo</a>
          <% } %>
        </div>
    </div> 

  </body>
</html>
